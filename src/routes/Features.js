import React from 'react';
import Index from '../components/headers/Index';

function Features() {
  return (
    <div>
        <Index />
        <h1>Esto funciona / features</h1>
    </div>
  )
};

export default Features;